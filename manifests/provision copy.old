Package {
  provider => chocolatey
}

# ensure that my user is present and an Admin (maybe with a different pwd)
user { $hostuser : # look in VagrantFile, this is set to the user running the host; clever eh?
  ensure => "present",
  groups => ["Administrators"],
  password => "password"
}

# install boxstarter
package { "boxstarter" :
  ensure => "installed"
}

# other packages
package { "fiddler4" :
  ensure => "installed"
}

package { "git" :
  ensure => "installed"
}

package { "git-credential-winstore" :
  ensure => "installed"
}

package { "poshgit" :
  ensure => "installed"
}

package { "sublimetext2" :
  ensure => "installed"
}

package { "7zip" :
  ensure => "installed"
}

package { "WebPi" :
  ensure => "installed"
}

package { "dotpeek" :
  ensure => "installed"
}

package { "ruby" :
  ensure => "installed"
}

package { "ruby.devkit" :
  ensure => "installed"
}

package { "GoogleChrome" :
  ensure => "installed"
}

package { "p4merge" :
  ensure => "installed"
}

package { "adobereader" :
  ensure => "installed"
}

package { "resharper" :
  ensure => "installed"
}

package { "xamarin-studio" :
  ensure => "installed"
}

package { "xamarin-visualstudio" :
  ensure => "installed"
}

# ... other packages here...